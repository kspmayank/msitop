<%= form_for(@secondary_menu) do |f| %>
  <% if @secondary_menu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@secondary_menu.errors.count, "error") %> prohibited this secondary_menu from being saved:</h2>

      <ul>
      <% @secondary_menu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :link %><br>
    <%= f.text_field :link %>
  </div>
  <div class="field">
    <%= f.label :order %><br>
    <%= f.number_field :order %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
